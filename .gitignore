node_modules/
dist/
.next/
.turbo/
*.tsbuildinfo
# **/ is load-bearing: a pattern with a slash inside it is anchored to this
# file's directory, so plain `src/generated/` only ever matched <root>/src/generated
# and never apps/api/src/generated (the generated Prisma client).
**/src/generated/
.env
.env.*
!.env.example

# local-only project config (MCP servers) — not shared to the repo
.mcp.json

# local planning docs (superpowers SDD)
docs/
CLAUDE.md
docs/coding-conventions.md


# superpowers brainstorm mockups (local only)
.superpowers/
